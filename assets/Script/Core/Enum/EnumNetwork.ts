// 网络状态

export enum EnumSocketStatus {
    connecting = 0,
    open = 1,
    closing = 2,
    closed = 3
}

export enum EnumNetworkName {
    Default = "Default",
    Dispatch = "Dispatch"
}