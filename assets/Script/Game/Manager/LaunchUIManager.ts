import UIManager from "../../Core/Manager/UIManager";

const { ccclass, property } = cc._decorator;

@ccclass
export default class LaunchUIManager extends UIManager {

    name: string = "LaunchUIManager";

    onLoad() {

    }

    onDestroy() {

    }
}
